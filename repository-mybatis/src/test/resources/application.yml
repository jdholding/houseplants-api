spring:
  datasource:
    hikari:
      maximum-pool-size: 5

mybatis:
  config-location: classpath:mybatis-config.xml

logging:
  level:
    rc:
      holding:
        houseplants:
          repository:
            mybatis: DEBUG
